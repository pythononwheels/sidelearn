# Dependencies
node_modules/

# Python (server/)
**/.venv/
**/__pycache__/
*.db

# Build output
.output/
.wxt/
stats.html

# Playwright E2E artifacts (browsers live in a global cache, not here)
test-results/
playwright-report/
tests/e2e/__screens__/

# Data — downloaded sources (regenerate via `npm run data:wordlists`).
# The small sample files in public/data/ ARE committed so the extension runs on clone.
data/sources/

# Env / secrets
.env
.env.*
!.env.example

# OS / editor
.DS_Store
*.log
.idea/
.vscode/

# Caddy site snippets are documented in the READMEs, not committed as config.
*.caddy
